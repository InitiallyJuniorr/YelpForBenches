import React from 'react'
import ReactDOM from 'react-dom/client'
import Login from './Login' // This is the file you wrote earlier

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Login />
  </React.StrictMode>
)